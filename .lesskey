# to regenerate this file:
#       lesskey -o .less .lesskey
#
# to regenerate the .less.empty:
#       lesskey -o .less.empty <(echo)


#command

# this is REALLY useful in combination with 'lgrep' -- it lets you rapidly scan through matching files
]       next-file
[       prev-file


#env

LESS = -iR

# make UTF8 characters display properly
LESSCHARSET = utf-8
LESSUTFBINFMT = *n      #  go ahead and output to the terminal even those characters that it does not consider safe
